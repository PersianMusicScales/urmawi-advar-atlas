import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const must=["index.html","manifest.webmanifest","service-worker.js","offline.html","css/app.css","css/pwa-responsive.css","js/app.js","js/pwa.js","assets/brand/advar-mark.svg","assets/icons/icon-192x192.png","assets/icons/icon-512x512.png"];const errors=[];for(const f of must)if(!fs.existsSync(path.join(root,f)))errors.push(`missing:${f}`);const html=fs.readFileSync(path.join(root,"index.html"),"utf8");for(const token of ["manifest.webmanifest","mobileNav","welcomeScreen","mobileInstrumentToggle","js/pwa.js"])if(!html.includes(token))errors.push(`html:${token}`);const manifest=JSON.parse(fs.readFileSync(path.join(root,"manifest.webmanifest"),"utf8"));if(manifest.icons.length<9)errors.push("manifest:icons");if(!manifest.screenshots?.length)errors.push("manifest:screenshots");const pwa=fs.readFileSync(path.join(root,"js/pwa.js"),"utf8");for(const lang of ["en","fa","ar","tr"])if(!pwa.includes(`${lang}:{`))errors.push(`pwa-language:${lang}`);if(errors.length){console.error(errors.join("\n"));process.exit(1)}console.log("PWA structure, manifest, responsive UI, and four-language extension checks passed.");